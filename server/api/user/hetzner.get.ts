export default defineEventHandler(async (event) => {
  // try {
  //   const result = await addNewCertToLoadBalancer('emmaherbst.de')

  //   return result
  // }
  // catch (error) {
  //   console.error('Error adding certificate to load balancer:', error)
  // }
})
